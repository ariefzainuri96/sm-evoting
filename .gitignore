# --- GLOBAL IGNORES ---
**/node_modules/
.env

# --- SMART CONTRACT APP (Foundry) ---
# Ignore the compiler output and cache in any subfolder
**/cache/
**/out/

# Broadcast Logs Logic:
# We usually want to KEEP the summary files but IGNORE sensitive details or local runs
**/broadcast/
# BUT we "unignore" the folder so we can store the high-level deployment JSONs
!**/broadcast/
# Ignore sensitive or temporary broadcast data:
**/broadcast/*/31337/
**/broadcast/**/dry-run/

# --- SUBGRAPH APP ---
# Ignore the auto-generated code and the built WASM files
**/generated/
**/build/

# --- GENERAL ---
docs/
.DS_Store